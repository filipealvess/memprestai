import styled from 'styled-components';

const CheckBoxWrapper = styled.article`
  display: flex;
  align-items: center;
`;

export const Box = styled.input`
  margin-right: 10px;
`;

export default CheckBoxWrapper;
